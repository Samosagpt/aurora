# Core dependencies
streamlit>=1.28.0
# PyTorch will be installed separately based on CUDA availability
diffusers>=0.24.0
transformers>=4.35.0
accelerate>=0.24.0

# AI and ML
openai-whisper>=20231117
ollama>=0.1.7
huggingface-hub>=0.19.0
opencv-python>=2.2.6

# Audio processing - requires PortAudio system library
sounddevice>=0.4.6; sys_platform == "win32"  # Requires portaudio system library
soundfile>=0.12.1; sys_platform == "win32"  # Audio file I/O
pyaudio>=0.2.14; sys_platform == "win32"  # Requires portaudio system library - Windows only
scipy>=1.11.0
# Note: Bark TTS must be installed separately from GitHub
# pip install git+https://github.com/suno-ai/bark.git
pyttsx3>=2.90

# Attachment handling (Images & PDFs)
Pillow>=10.0.0
PyPDF2>=3.0.0
pdf2image>=1.16.3
# Note: pdf2image also requires poppler-utils to be installed:
# Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases/
# Linux: sudo apt-get install poppler-utils
# macOS: brew install poppler

# Desktop control and automation
pyautogui>=0.9.54; sys_platform == "win32"  # Desktop automation - primarily for desktop use
# Windows-only: pywin32 is required for Win32 API integrations on Windows desktops.
# Make this platform specific so Linux-based hosts (like Streamlit Cloud) won't try
# to install it and fail due to missing wheels.
pywin32>=306; sys_platform == "win32"
keyboard>=0.13.5; sys_platform == "win32"  # Keyboard control - requires root/admin on Linux
mouse>=0.7.1; sys_platform == "win32"  # Mouse control - requires root/admin on Linux
pytesseract>=0.3.10  # OCR for text detection in screenshots
easyocr>=1.7.0  # Enhanced OCR with deep learning

# Web and API
requests>=2.31.0
wikipedia>=1.4.0

# System and utilities
numpy>=1.24.0
python-dotenv>=1.0.0
psutil>=5.9.0

# Production dependencies
gunicorn>=21.0.0
uvicorn>=0.23.0
fastapi>=0.104.0
redis>=5.0.0
celery>=5.3.0
npm>=0.1.1

# Monitoring and logging
prometheus-client>=0.19.0
structlog>=23.2.0
sentry-sdk>=1.38.0

# Security
cryptography>=41.0.0
bcrypt>=4.1.0
passlib>=1.7.4

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
coverage>=7.3.0

# Development and packaging
pyinstaller>=6.0.0
auto-py-to-exe>=2.44.0

# Optional: for enhanced features
ffmpeg-python>=0.2.0
webrtcvad>=2.0.10

# Database (optional)
sqlalchemy>=2.0.0
alembic>=1.12.0

# Caching
diskcache>=5.6.0

# Configuration validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
